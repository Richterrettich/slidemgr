require 'spec_helper'

describe CommandRouter do
  it 'has a version number' do
    expect(CommandRouter::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
